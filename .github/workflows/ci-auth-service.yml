name: CI - Auth Service

on:
  push:
    branches: [main, develop]
    paths:
      - 'services/auth-service/**'
      - 'src/Shared/**'
      - '.github/workflows/ci-auth-service.yml'
  pull_request:
    branches: [main, develop]
    paths:
      - 'services/auth-service/**'
      - 'src/Shared/**'

jobs:
  build-and-test:
    name: Build Auth Service
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'
      
      - name: Restore dependencies
        run: dotnet restore services/auth-service/src/AuthService.csproj
      
      - name: Build
        run: dotnet build services/auth-service/src/AuthService.csproj --configuration Release --no-restore
      
      - name: Run tests (if exist)
        run: |
          if [ -d "services/auth-service/tests" ]; then
            dotnet test services/auth-service/tests --configuration Release --no-build --verbosity normal
          else
            echo "No tests found, skipping..."
          fi
        continue-on-error: true
      
      - name: Build Docker image (validation only)
        run: docker build -t auth-service:ci -f services/auth-service/Dockerfile services/auth-service
