name: CI - User Service

on:
  push:
    branches: [main, develop]
    paths:
      - 'services/user-service/**'
      - 'src/Shared/**'
      - '.github/workflows/ci-user-service.yml'
  pull_request:
    branches: [main, develop]
    paths:
      - 'services/user-service/**'
      - 'src/Shared/**'

jobs:
  build-and-test:
    name: Build User Service
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'
      
      - name: Restore dependencies
        run: dotnet restore services/user-service/src/UserService/UserService.csproj
      
      - name: Build
        run: dotnet build services/user-service/src/UserService/UserService.csproj --configuration Release --no-restore
      
      - name: Run tests (if exist)
        run: |
          if [ -d "services/user-service/tests" ]; then
            dotnet test services/user-service/tests --configuration Release --no-build --verbosity normal
          else
            echo "No tests found, skipping..."
          fi
        continue-on-error: true
      
      - name: Build Docker image (validation only)
        run: docker build -t user-service:ci -f services/user-service/Dockerfile services/user-service
