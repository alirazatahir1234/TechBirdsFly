name: CI - Event Bus Service

on:
  push:
    branches: [main, develop]
    paths:
      - 'services/event-bus-service/**'
      - 'services/cache-service/src/CacheClient/**'
      - 'src/Shared/**'
      - '.github/workflows/ci-event-bus-service.yml'
  pull_request:
    branches: [main, develop]
    paths:
      - 'services/event-bus-service/**'
      - 'services/cache-service/src/CacheClient/**'
      - 'src/Shared/**'

jobs:
  build-and-test:
    name: Build Event Bus Service
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'
      
      - name: Restore dependencies
        run: dotnet restore services/event-bus-service/src/EventBusService.csproj
      
      - name: Build
        run: dotnet build services/event-bus-service/src/EventBusService.csproj --configuration Release --no-restore
      
      - name: Run tests (if exist)
        run: |
          if [ -d "services/event-bus-service/tests" ]; then
            dotnet test services/event-bus-service/tests --configuration Release --no-build --verbosity normal
          else
            echo "No tests found, skipping..."
          fi
        continue-on-error: true
      
      - name: Build Docker image (validation only)
        run: docker build -t event-bus-service:ci -f services/event-bus-service/Dockerfile .
